{%- if twewyart.title -%}
    {{ twewyart.title }}
{%- elif ocs.title -%}
    {{ ocs.title }}
{%- elif artwork.title -%}
    {{ artwork.title }}
{%- elif story.Title -%}
    {{ story.Title }}
{%- elif posts.PostDate -%}
    {{ posts.PostDate}}
{%- elif RelTWEWY.SeriesName -%}
    {{RelTWEWY.SeriesName}}
{%- else -%}
    {{ title }}
{%- endif -%}