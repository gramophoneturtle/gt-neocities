{%- if twewyart.title -%}
    {{ twewyart.title }}
{%- elif ocs.title -%}
    {{ ocs.title }}
{%- elif artwork.title -%}
    {{ artwork.title }}
{%- elif story.Title -%}
    {{ story.Title }}
{%- else -%}
    {{ title }}
{%- endif -%}